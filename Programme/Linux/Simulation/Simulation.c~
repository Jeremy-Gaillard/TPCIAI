#include <stdio.h>
#include <stdlib.h>
#include "../config.h"

int main()
{
	int bal_log_disque = mq_open( BALDIS, O_RDONLY );
	char commande[20];
	for( ; ; )
	{
		scanf("%s", commande);
		if(commande == "logd")
		{
			
		}
		if(commande == "q")
		{
			break;
		}
	}
	return 0;
}
